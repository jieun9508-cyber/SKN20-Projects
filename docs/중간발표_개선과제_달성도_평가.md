# 중간 발표 개선 과제 달성도 평가 (코드 근거 기반)

> 평가일: 2026-03-06
> 평가 방식: 코드 직접 검증 (추측 배제)

---

## 1. DB 구조 — 달성도: 90%

### 중간 발표 때 과제
- **현재**: Practice/User/UserDetail 중심의 단순 구조, 일부 로컬 JSON과 병행 사용
- **문제점**: 데이터 소스 이원화로 확장/버전관리 어려움, 신규 콘텐츠 추가 시 수작업 많음
- **해결 방안**: 단일 User 모델로 통합, DB 통합 스키마로 이동

### 달성 현황

| 과제 | 상태 | 코드 근거 |
|------|------|----------|
| 단일 User 모델 통합 | 완료 | `user_model.py` — UserProfile(BigAutoField PK) + UserDetail(1:1). 단, UserDetail 필드(`is_developer`, `job_role`, `interests`)가 별도 테이블로 남아있어 완전 병합은 아님 |
| DB 통합 스키마 이동 | 완료 | 11개 migration 파일로 버전 관리. PostgreSQL 기반 10개+ 테이블 구축 (gym_user, gym_practice, gym_user_activity 등) |
| 로컬 JSON 의존 해소 | 거의 완료 | **Architecture**: `SystemArchitecturePractice.vue:381` — `fetch('/api/core/practices/unit03/')` DB 100% 사용. **BugHunt**: `BugHunt.vue:3232` — `axios.get('/api/core/practices/unit02/')` DB 100% 사용. 프론트 JSON 파일은 import 안 됨(레거시). 단, 백엔드 `practice_view.py:41-55`에서 `progressive-problems.json`을 interview_rubric 보충용 폴백으로 참조 |
| 신규 콘텐츠 추가 용이 | 완료 | `PracticeDetail.content_data`(JSONField)로 UI 수정 없이 DB만 수정하면 반영 가능 |

### DB 스키마 현황

```
backend/core/models/
├── user_model.py
│   ├── UserProfile (PK: BigAutoField, Unique: username)
│   └── UserDetail (OneToOne -> UserProfile)
├── practice_model.py
│   ├── Practice (PK: CharField 'unit0X')
│   └── PracticeDetail (PK: CharField 'unit0X0Y', FK -> Practice, content_data: JSONField)
├── activity_model.py
│   ├── UserActivity (OneToOne -> UserProfile, total_points, current_rank)
│   ├── UserSolvedProblem (FK -> UserProfile, FK -> PracticeDetail, submitted_data: JSONField)
│   ├── UserProgress (FK -> UserProfile, FK -> Practice, unlocked_nodes: JSONField)
│   ├── UserAvatar (FK -> UserProfile)
│   ├── UserBattleRecord (OneToOne -> UserProfile, win/draw/lose)
│   └── UserWarsScore (FK -> UserProfile, game_type, score)
├── interview_model.py (SavedJobPosting, InterviewSession, InterviewTurn, InterviewFeedback)
├── coach_model.py (CoachConversation, CoachMessage)
└── question_bank_model.py (InterviewQuestion)
```

### 남은 점
- 백엔드 BugHunt interview_rubric 폴백 JSON 참조 1건 (`practice_view.py:41-55`)
- UserDetail이 별도 테이블로 잔존 (UserProfile에 병합 가능)

---

## 2. 기능 확장 — 달성도: 90%

### 중간 발표 때 과제
- **현재**: 3개의 Practice 완료, Practice 진도가 로컬에만 저장, Practice별 UI 상이
- **문제점**: 신규 Practice 추가 시 UI/데이터 모두 수정 필요, 브라우저 초기화 시 진도 소멸, UI 통일성 부족
- **해결 방안**: 백엔드에 진도 저장, UI 통합
- **(참고)** Ops, Agent Practice는 사정상 미구현. 대신 기존 Practice 고도화 및 유저 대전, Job Planner, Mock Interview로 대체

### 달성 현황

| 과제 | 상태 | 코드 근거 |
|------|------|----------|
| 백엔드 진도 저장 | 완료 | `progress.js:23` — `axios.get('/api/core/activity/progress/')`, `progress.js:62` — `axios.post('/api/core/activity/progress/')`, `progress.js:92` — `axios.post('/api/core/activity/submit/')`. **진도 관련 localStorage 사용: 0건** |
| localStorage 잔존 | 비핵심만 | BugHunt/Pseudocode/Architecture에서 **튜토리얼 스킵 플래그**용으로만 사용 (UI 상태). Job Planner는 분석 결과를 localStorage에 캐싱 |
| UI 통일성 | 부분 달성 | 공통 컴포넌트 `PracticeExitConfirmModal` — 3개 Practice 모두에서 import 확인 (bughunt:1152, pseudocode:543, architecture:163). `AnalysisLoadingScreen` — Pseudocode에서 사용. 그러나 각 Practice 메인 레이아웃 패턴 상이 |
| Wars (유저 대전) | 완료 | 3개 미니게임(LogicRun, BugBubbleMonster, ArchDrawQuiz). Socket.io 실시간 통신(`socket_server.py`). 전적 DB 저장(`UserBattleRecord`, `UserWarsScore`). 포인트/등급 연동(`wars_score_view.py:80-93`) |
| Job Planner | 완료 | 백엔드 7개 API 엔드포인트 완성(parse/analyze/company-analyze/agent-report/recommend/parse-resume/review-portfolio). 프론트 모달 UI 구현. 분석 결과는 localStorage 캐시 — 이력서/포트폴리오 등 개인정보 특성상 서버 DB 영구 저장 대신 클라이언트 측 임시 저장을 의도적으로 선택(개인정보 서버 유출 위험 방지, 분석 내용 매회 갱신 특성) |
| Mock Interview | 완료 | STT(`stt_view.py` — Whisper 기반), TTS(`tts.js` — OpenAI nova 음성), WebRTC(`useWebRTC.js`), 세션 관리(InterviewSession/InterviewTurn/InterviewFeedback 모델), 피드백 DB 저장 |

### localStorage 전수 조사 결과

| 파일 | 용도 | 진도 관련 여부 |
|------|------|--------------|
| BugHunt.vue:1469 | 튜토리얼 스킵 플래그 | X (UI 상태) |
| PseudocodePractice.vue:600 | 튜토리얼 스킵 플래그 | X (UI 상태) |
| SystemArchitecturePractice.vue:336 | 튜토리얼 스킵 플래그 | X (UI 상태) |
| JobPlannerModal.vue:1925,1968,1990,2024 | 프로필/분석결과 임시 캐시 | X (비핵심) |
| JobPlannerHistory.vue:185,195 | 분석 기록 캐시 | X (비핵심) |

### 남은 점
- Practice별 메인 레이아웃 패턴 통일 미완

---

## 3. AI 코치, 평가 성능 — 달성도: 95%

### 중간 발표 때 과제
- **현재**: LLM 프롬프트 기반 단순 평가 방식, 코드 실행 불가
- **문제점**: 평가 일관성 부족, 환각 발생, 응답 속도 지연, 학습 데이터 없음
- **해결 방안**: 하이브리드 평가, 개인화 피드백

### 달성 현황

| 과제 | 상태 | 코드 근거 |
|------|------|----------|
| 하이브리드 평가 | 완료 | **의사코드**: `pseudocode_evaluator.py:699-758` — Rule 검증(0-15점) + LLM 평가(0-85점) = 100점. **아키텍처**: `architectureValidator.js:372-436` — 3단계 규칙 검증 후 `architecture_view.py:456-563`에서 LLM 루브릭 평가 |
| 평가 일관성 | 대부분 통과 | `validation_report.md` — 20샘플x5회=100회 검증. 일관성 sigma_avg=3.37(목표<=5 통과), 순위정확도 tau=0.800(목표>=0.75 통과), 수렴타당도 r=0.685(목표>=0.65 통과). **구분력 28.9pt만 목표 30pt에 1.1pt 미달** |
| 환각 방지 | 구현 | `coach_prompt.py:44-59` — `is_off_topic()` 가드레일(무의미 정규식, 욕설, 범위밖 키워드 필터). `response_format={"type":"json_object"}` 강제. 의사코드 `temperature=0.3`, 아키텍처 `temperature=0.7` |
| 개인화 피드백 | 완료 | `coach_tools.py:11-109` — 6개 도구(성적조회/약점분석/최근활동/문제추천/커리큘럼/학습가이드). `coach_tools.py:172-231`에서 `UserSolvedProblem.submitted_data`의 메트릭을 Unit별로 추출하여 맞춤 피드백 생성 |
| 코드 실행 | 완료 | `code_execution_view.py:17-145` — Docker 샌드박스(CPU 0.5, 메모리 256MB, 타임아웃 30초, 네트워크 비활성화). `pseudocode_execution.py:35-181` — 자동 테스트 코드 생성 |
| 학습 데이터 | 저장+활용 | `activity_model.py:83` — `submitted_data = JSONField`. Coach가 `coach_tools.py:172`에서 직접 조회하여 약점 분석에 활용. 대화 히스토리 `CoachConversation`/`CoachMessage` 모델로 DB 영속 |

### 평가 검증 수치

| 지표 | Bug Hunt | Architecture | 목표 | 판정 |
|------|----------|-------------|------|------|
| 일관성 (sigma_avg) | 1.78 | 3.37 | <= 5.0 | 통과 |
| 구분력 | 82.4pt | 28.9pt | >= 30pt | 통과 / 1.1pt 미달 |
| 순위 정확도 (Kendall tau) | 0.983 | 0.800 | >= 0.75 | 통과 |

### 남은 점
- 아키텍처 구분력 1.1pt 미달 (구조적 원인: 6개 Well-Architected 축이 범용 기준)

---

## 4. 보안 — 달성도: 80%

### 중간 발표 때 과제
- **현재**: AI API 노출 위험, 본인 체크 없음
- **문제점**: 키 유출/오용 가능성, 다른 유저 정보 수정 가능, 프롬프트 인젝션/응답 위변조 가능
- **해결 방안**: 사용자 인증 필수화, Rate Limiting 적용, API 키 백엔드로 이동

### 달성 현황

| 과제 | 상태 | 코드 근거 |
|------|------|----------|
| API 키 백엔드 이동 | 완료 | `ai_proxy_view.py:34` — `api_key = settings.OPENAI_API_KEY`. `settings.py:195` — `env('OPENAI_API_KEY')`. 프론트에서 직접 AI API 호출 코드 없음 |
| 사용자 인증 필수화 | 대부분 완료 | **핵심 AI 뷰 6개 IsAuthenticated 전환**: AIProxyView(`ai_proxy_view.py:22`), ArchitectureEvaluationView(`architecture_view.py:469`), ArchitectureQuestionGeneratorView(`architecture_view.py:580`), BugHuntInterviewView(`bughunt_interview_view.py:26`), BugHuntEvaluationView(`bughunt_evaluation_view.py:26`), TTSView(`tts_view.py:47`). 기존 IsAuthenticated: Coach(`coach_view.py:134`), Pseudocode(`pseudocode_evaluation.py:88`), PseudocodeAgent(`pseudocode_agent_view.py:44`), WarsScore(`wars_score_view.py:39`). **잔여 AllowAny**: 면접 세션(session_view 4개, answer_view, stt_view), JobPlanner(7개), CodeExecution(2개), 기타(activity, common, youtube 등) |
| 본인 확인 | 부분 적용 | `user_view.py:284-298` — IsOwner 이메일 기반으로 복원(2026-03-06). `user_view.py:371-375` — UserProfileViewSet의 update/partial_update/destroy에 `[IsAuthenticated(), IsOwner()]` 적용. 관리자(is_staff)는 항상 허용 |
| Rate Limiting | 완료 | `settings.py:143-153` — 3계층 Rate Limiting 전면 적용. 글로벌: `AnonRateThrottle`(30회/분) + `UserRateThrottle`(100회/분). AI 전용: `ScopedRateThrottle` scope='ai'(10회/분) — AI 호출 뷰 20개+에 개별 적용. 로그인: scope='login'(5회/분) — `auth_view.py:25-26` |
| 프롬프트 인젝션 방지 | Coach만 | `coach_prompt.py:44-59`에 `is_off_topic()` 가드레일 적용(`coach_view.py:156`). JobPlanner 입력 15000자 제한(`job_planner_view.py:413`). 그 외 AI 뷰는 사용자 입력을 검증 없이 LLM에 직접 전달 |

### Rate Limiting 적용 현황

| 스코프 | 제한 | 적용 대상 | 코드 근거 |
|--------|------|----------|----------|
| anon (글로벌) | 30회/분 | 비로그인 전체 API | `settings.py:149` — DEFAULT_THROTTLE_CLASSES |
| user (글로벌) | 100회/분 | 로그인 전체 API | `settings.py:150` — DEFAULT_THROTTLE_CLASSES |
| ai (스코프) | 10회/분 | AI 호출 뷰 전체 | AIProxyView(`ai_proxy_view.py:24-25`), BugHuntEvaluationView(`bughunt_evaluation_view.py:28-29`), BugHuntInterviewView(`bughunt_interview_view.py:28-29`), ArchitectureEvaluationView(`architecture_view.py:471-472`), ArchitectureQuestionGeneratorView(`architecture_view.py:582-583`), CoachSessionView(`coach_view.py:136-137`), AnswerView(`answer_view.py:266-267`), InterviewFeedbackView(`session_view.py:335-336`), InterviewHistoryView(`session_view.py:368-369`), STTView(`stt_view.py:50-51`), TTSView(`tts_view.py:49-50`), PseudocodeEvaluation(`pseudocode_evaluation.py:89` — AIRateThrottle 커스텀 클래스), PseudocodeAgentView(`pseudocode_agent_view.py:46-47`), JobPlanner 7개 뷰(`job_planner_view.py:75-76,495-496,1227-1228,1500-1501,2237-2238,2438-2439,2854-2855`) |
| login (스코프) | 5회/분 | 로그인 시도 | LoginView(`auth_view.py:25-26`) |

### 인증 적용 현황

| 엔드포인트 | 권한 | Rate Limit | 위험도 |
|-----------|------|-----------|--------|
| AIProxyView | **IsAuthenticated** | ai: 10회/분 | 낮음 |
| BugHuntEvaluationView | **IsAuthenticated** | ai: 10회/분 | 낮음 |
| BugHuntInterviewView | **IsAuthenticated** | ai: 10회/분 | 낮음 |
| ArchitectureEvaluationView | **IsAuthenticated** | ai: 10회/분 | 낮음 |
| ArchitectureQuestionGeneratorView | **IsAuthenticated** | ai: 10회/분 | 낮음 |
| TTSView | **IsAuthenticated** | ai: 10회/분 | 낮음 |
| AICoachView | IsAuthenticated | ai: 10회/분 | 낮음 |
| PseudocodeEvaluationView | IsAuthenticated | ai: 10회/분 | 낮음 |
| PseudocodeAgentView | IsAuthenticated | ai: 10회/분 | 낮음 |
| WarsScoreView | IsAuthenticated | user: 100회/분 | 낮음 |
| UserProfileViewSet (수정/삭제) | IsAuthenticated + IsOwner | user: 100회/분 | 낮음 |
| 면접 세션 (session_view 4개, answer_view) | AllowAny | ai: 10회/분 | 중간 |
| STTView | AllowAny | ai: 10회/분 | 중간 |
| CodeExecutionView (2개) | AllowAny | anon: 30회/분 | 중간 |
| JobPlanner 전체 (7개 뷰) | AllowAny | ai: 10회/분 | 중간 |
| LoginView | AllowAny | login: 5회/분 | 낮음 (브루트포스 방지) |

### 개선된 점 (2026-03-06)
- **핵심 AI 뷰 6개 IsAuthenticated 전환**: AIProxyView(`ai_proxy_view.py:22`), ArchitectureEvaluationView(`architecture_view.py:469`), ArchitectureQuestionGeneratorView(`architecture_view.py:580`), BugHuntInterviewView(`bughunt_interview_view.py:26`), BugHuntEvaluationView(`bughunt_evaluation_view.py:26`), TTSView(`tts_view.py:47`). 비로그인 사용자의 AI API 무단 호출 차단
- **Rate Limiting 3계층 전면 적용**: `settings.py:143-153` — 글로벌(anon 30회/분, user 100회/분) + AI 전용(10회/분) + 로그인(5회/분). AI 비용 폭탄, DDoS, 브루트포스 공격 방지
- **AI 뷰 개별 ScopedRateThrottle 적용**: 20개+ AI 호출 뷰에 `throttle_scope = 'ai'` 개별 설정. 함수형 뷰(`pseudocode_evaluation.py:21`)는 `AIRateThrottle` 커스텀 클래스로 대응
- **IsOwner 이메일 기반 복원**: `user_view.py:284-298` — `obj.email == request.user.email`로 소유자 검증, 관리자 바이패스 포함

### 남은 점
- 해결 방안 3가지(인증 필수화, Rate Limiting, API 키 이동) **모두 완료** (인증은 핵심 AI 뷰 완료, 면접/JobPlanner 일부 잔여)
- 면접 세션, STT, JobPlanner, CodeExecution 등 일부 뷰 AllowAny 잔존 (단, Rate Limiting으로 남용 방지)
- 프롬프트 인젝션 방지가 Coach 외에는 미적용

---

## 종합 요약

| 영역 | 달성도 | 핵심 성과 | 미달 사항 |
|------|--------|----------|----------|
| DB 구조 | 90% | 3개 Practice 모두 DB API 전환 완료, 11개 migration 버전 관리 | 백엔드 BugHunt 폴백 JSON 1건 |
| 기능 확장 | 90% | 진도 100% 서버 이전, Wars/Interview 풀스택 구현, Job Planner 완성(개인정보 보호 설계) | Practice별 UI 레이아웃 통일 미완 |
| AI 코치/평가 | 95% | Rule+LLM 하이브리드, Docker 코드 실행, 학습 데이터 활용 | 아키텍처 구분력 1.1pt 미달 |
| 보안 | 80% | API 키 백엔드 이동, 핵심 AI 뷰 6개 IsAuthenticated 전환, Rate Limiting 3계층 전면 적용, IsOwner 복원 | 면접/JobPlanner 일부 AllowAny 잔존, 프롬프트 인젝션 방지 미흡 |
